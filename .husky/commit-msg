#!/usr/bin/env sh
# commit-msg hook — Conventional Commits formatını zorunlu kılar.
# Yapılandırma: root package.json → "commitlint" anahtarı.
. "$(dirname -- "$0")/_/husky.sh"
npx --no -- commitlint --edit "$1"
